workflows:
  ios-build:
    name: Build NRO Unsigned IPA
    instance_type: mac_mini_m2
    environment:
      xcode: latest
    scripts:
      - name: Build Unsigned App (Kèm Trị Bệnh Tận Gốc)
        script: |
          # 1. Sửa lỗi kí tự ẩn CRLF của Windows cản trở Mac chạy script
          find . -type f -name "*.sh" -exec sed -i '' 's/\r$//' {} \;
          
          # 2. Cấp quyền bạo lực cho nhân Unity
          chmod -R 777 .
          find . -type f -name "il2cpp" -exec chmod +x {} \;
          find . -type f -name "MapFileParser" -exec chmod +x {} \;
          
          # 3. Ép dùng Python giả lập (Đặt chung bước này để không bị mất môi trường)
          mkdir -p $HOME/bin
          ln -sf $(which python3) $HOME/bin/python
          export PATH="$HOME/bin:$PATH"
          
          # 4. Bắt đầu Build Game
          xcodebuild build \
            -project "Unity-iPhone.xcodeproj" \
            -scheme "Unity-iPhone" \
            -sdk iphoneos \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ENABLE_USER_SCRIPT_SANDBOXING=NO \
            OTHER_LDFLAGS="-Wl,-ld_classic" \
            CONFIGURATION_BUILD_DIR=$CM_BUILD_DIR/build_output
            
      - name: Phẫu thuật đổi tên thành NRO, thêm Privacy và đóng gói IPA
        script: |
          cd $CM_BUILD_DIR/build_output
          
          # Tìm thư mục app
          APP_DIR=$(find . -name "*.app" -type d | head -n 1)
          APP_NAME=$(basename "$APP_DIR" .app)
          
          # Đổi tên cái lõi binary và Info.plist
          mv "$APP_DIR/$APP_NAME" "$APP_DIR/NRO"
          plutil -replace CFBundleExecutable -string "NRO" "$APP_DIR/Info.plist"
          plutil -replace CFBundleName -string "NRO" "$APP_DIR/Info.plist"
          
          # Bơm PrivacyInfo lách luật của Apple
          cat <<EOF > "$APP_DIR/Frameworks/UnityFramework.framework/PrivacyInfo.xcprivacy"
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>NSPrivacyTracking</key>
              <false/>
              <key>NSPrivacyCollectedDataTypes</key>
              <array/>
              <key>NSPrivacyAccessedAPITypes</key>
              <array>
                  <dict>
                      <key>NSPrivacyAccessedAPIType</key>
                      <string>NSPrivacyAccessedAPICategorySystemBootTime</string>
                      <key>NSPrivacyAccessedAPITypeReasons</key>
                      <array><string>35F9.1</string></array>
                  </dict>
                  <dict>
                      <key>NSPrivacyAccessedAPIType</key>
                      <string>NSPrivacyAccessedAPICategoryFileTimestamp</string>
                      <key>NSPrivacyAccessedAPITypeReasons</key>
                      <array><string>C617.1</string></array>
                  </dict>
                  <dict>
                      <key>NSPrivacyAccessedAPIType</key>
                      <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
                      <key>NSPrivacyAccessedAPITypeReasons</key>
                      <array><string>CA92.1</string></array>
                  </dict>
              </array>
          </dict>
          </plist>
          EOF
          
          # Đổi tên vỏ bọc bên ngoài
          mv "$APP_DIR" "NRO.app"
          
          # Đóng gói IPA
          mkdir -p $CM_BUILD_DIR/build/ios/ipa/Payload
          cp -r "NRO.app" $CM_BUILD_DIR/build/ios/ipa/Payload/
          cd $CM_BUILD_DIR/build/ios/ipa
          zip -qr NRO_Unsigned.ipa Payload
    artifacts:
      - build/ios/ipa/*.ipa